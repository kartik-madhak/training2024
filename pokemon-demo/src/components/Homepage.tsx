import {PokemonDetails} from "./PokemonDetails";

export const Homepage = () => {
    return (
        <div>
            <h1>Pokemon Homepage</h1>
            <PokemonDetails/>
        </div>
    );
}